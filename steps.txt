template for creating project
django-admin startproject backend --template https://github.com/mongodb-labs/django-mongodb-project/archive/refs/heads/5.0.x.zip

template for creating app
python manage.py startapp accounts --template https://github.com/mongodb-labs/django-mongodb-app/archive/refs/heads/5.0.x.zip

-------

import os.path
from datetime import timedelta
from pathlib import Path
import environ

env = environ.Env()
BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))

environ.Env.read_env(os.path.join(BASE_DIR, '.env'))

---------
mongo uri :
MONGO_URI="mongodb+srv://admin:admin@gesture-soundboard.3qytn.mongodb.net/Gesture-Soundboard?retryWrites=true&w=majority&appName=Gesture-Soundboard"
---------

CORS_ALLOW_ALL_ORIGINS = True

CORS_ALLOW_METHODS = (
    "DELETE",
    "GET",
    "OPTIONS",
    "PATCH",
    "POST",
    "PUT",
)

-------

make vercel.json

{
    "builds": [{
        "src": "backend/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.12" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "backend/wsgi.py"
        }
    ]
}

------
add app=application to backend/wsgi.py

-----------

add _id = models.CharField(max_length=24, primary_key=True, default=lambda: str(ObjectId())) to models for DRF

-----------
add environment variables to vercel
add "whitenoise.middleware.WhiteNoiseMiddleware", to the middleware in settings
