import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gesturesoundboard21307',
  appName: 'frontend',
  webDir: 'dist/browser'
};

export default config;
